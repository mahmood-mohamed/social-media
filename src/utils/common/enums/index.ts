
export enum UserRoles {
    ADMIN = "admin",
    USER = "user",
    MODERATOR = "moderator"
}

export enum UserAgent {
    LOCAL = "local",
    GOOGLE = "google"
}

export enum Gender {
    MALE = "male",
    FEMALE = "female",
}
